# React_Tree
The HTL Skill Tree Web App is a React-based tool that visualizes school competencies as an interactive skill tree. It uses a JSON configuration to define skills and dependencies, automatically generates the layout, and allows users to view details with Markdown content and embedded videos.
